export type ProviderConfig = {
  token: string;
  owner: string;
  repo: string;
  baseUrl: string;
  repoUrl: string;
  prId: number;
  userAgent: string;
  timeZone: string;
  gitCloneBypass: boolean;
};
