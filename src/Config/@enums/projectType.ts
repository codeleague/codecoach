export enum ProjectType {
  // linters
  dotnetbuild = 'dotnetbuild',
  msbuild = 'msbuild',
  tslint = 'tslint',
  eslint = 'eslint',
  scalastyle = 'scalastyle',
  androidlint = 'androidlint',
  dartlint = 'dartlint',
  swiftlint = 'swiftlint',
  sarif = 'sarif',

  // copy paste detector
  jscpd = 'jscpd',
}
