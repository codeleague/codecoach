export type ProviderCustomConfigType = {
  owner: string;
  repo: string;
  token: string;
  apiUrl?: string;
  repoUrl?: string;
  prId: number;
  workDir?: string;
  userAgent?: string;
  timeZone?: string;
};
